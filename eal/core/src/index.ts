/**
 * Engine Abstraction Layer — core interfaces.
 *
 * Phase 0 placeholder: the EAL interfaces (WorldView, Renderer, InputSource,
 * Ticker, AudioSink, PresentationChannel) are specified in
 * docs/design/DESIGN.md §4 and implemented in Phase 1.
 */
export const EAL_VERSION = '0.1.0';
